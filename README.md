# nebula

the beyond backend

# configuration

all configuration files are located in `/config` and `/config/secure` is for the discord bot, dedicated gameserver api keys & vivox details for voice chat.

# todo

- [-] HWID Bans
- Quests (4.2 & 12.41)
- Arena Hype updating in-game
- Per Season playlists
- Per Season ContentPages

# setup

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run src/index.ts
```
